{
  "name": "anime-magnet-collector",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build.mjs",
    "dev": "bun run scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "verify:dist": "bun run scripts/verify-dist.mjs"
  },
  "devDependencies": {
    "@types/tampermonkey": "*",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3"
  }
}
